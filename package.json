{
  "name": "ar-location",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "yarn install && yarn http-server . -c-1 -o -S",
    "create-https-certificates": "openssl req -newkey rsa:2048 -new -nodes -x509 -days 3650 -keyout key.pem -out cert.pem"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
